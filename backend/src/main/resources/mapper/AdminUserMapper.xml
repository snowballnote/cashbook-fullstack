<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.cashbook.mapper.AdminUserMapper">

    <!-- 회원 목록 -->
    <select id="selectUserList" resultType="com.example.cashbook.dto.AdminUserListResponse">
        SELECT
            id
            , username
            , role
            , created_at AS createdAt
        FROM users
        ORDER BY id DESC
        LIMIT #{limit}
        OFFSET #{offset}
    </select>

    <!-- 전체 회원 수 -->
    <select id="countUsers" resultType="long">
        SELECT COUNT(*)
        FROM users
    </select>

</mapper>
